@import url("https://fonts.googleapis.com/css2?family=DM+Sans:wght@400;500;600;700&family=Noto+Sans+KR:wght@400;500;600;700;800&family=Playfair+Display:ital,wght@0,600;1,600&display=swap");

:root {
  --ivory: #f8f5ee;
  --paper: #fffdf8;
  --ink: #20231f;
  --forest: #354f43;
  --sage: #90a27e;
  --sage-pale: #dfe5d8;
  --blue: #a9bdd6;
  --blue-pale: #e5edf4;
  --blush: #efd8d1;
  --gold: #d3aa47;
  --line: #d9d6ce;
  --muted: #6c706a;
  --shell: 1360px;
  --radius: 18px;
  --shadow: 0 18px 50px rgba(37, 41, 35, 0.09);
  --font-display: "Playfair Display", Georgia, serif;
  --font-body: "DM Sans", system-ui, -apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif;
  --font-body-ko: Pretendard, "Noto Sans KR", "Apple SD Gothic Neo", "Malgun Gothic", sans-serif;
}

* {
  box-sizing: border-box;
}

html {
  scroll-behavior: smooth;
}

body {
  margin: 0;
  color: var(--ink);
  background: var(--ivory);
  font-family: var(--font-body);
  line-height: 1.5;
  -webkit-font-smoothing: antialiased;
}

body.overlay-open {
  overflow: hidden;
}

body.lang-ko {
  font-family: var(--font-body-ko);
  word-break: keep-all;
}

body.lang-ko :where(h3, p, a, button, input, select, textarea, label, span, strong, small, em, dt, dd, li, legend) {
  font-family: var(--font-body-ko) !important;
  letter-spacing: -0.025em;
}

body.lang-ko :where(h1, h2, h1 em, h2 em) {
  font-family: var(--font-body-ko) !important;
  letter-spacing: -0.04em;
}

body.lang-ko :where(h1 em, h2 em) {
  font-style: normal !important;
  transform: none !important;
}

body.lang-ko .wordmark {
  font-family: var(--font-body) !important;
  letter-spacing: -0.065em;
}

body.lang-ko .hero-copy h1 {
  font-size: clamp(2.75rem, 4.4vw, 4.6rem);
}

body.lang-ko .validation-heading h2 {
  font-size: clamp(2.1rem, 3.5vw, 3.5rem);
}

body.lang-ko :where(.section-top h2, .lookbook h2, .brand-story h2, .join-strip h2) {
  font-size: clamp(1.8rem, 2.7vw, 2.7rem);
}

body.lang-ko .ugc-heading h2 {
  font-size: clamp(2rem, 3.5vw, 3.5rem);
}

button,
input,
select,
textarea {
  font: inherit;
}

button,
a {
  -webkit-tap-highlight-color: transparent;
}

button {
  color: inherit;
}

a {
  color: inherit;
  text-decoration: none;
}

img {
  display: block;
  max-width: 100%;
}

h1,
h2,
h3,
p {
  margin-top: 0;
}

h1,
h2,
h3 {
  line-height: 1.08;
  letter-spacing: -0.045em;
}

h1,
h2 {
  font-family: var(--font-display);
  font-weight: 600;
}

.shell {
  width: min(calc(100% - 72px), var(--shell));
  margin-inline: auto;
}

.visually-hidden {
  position: absolute;
  width: 1px;
  height: 1px;
  padding: 0;
  margin: -1px;
  overflow: hidden;
  clip: rect(0, 0, 0, 0);
  white-space: nowrap;
  border: 0;
}

.skip-link {
  position: fixed;
  z-index: 1000;
  top: 12px;
  left: 12px;
  padding: 11px 16px;
  color: white;
  background: var(--ink);
  border-radius: 8px;
  transform: translateY(-150%);
}

.skip-link:focus {
  transform: translateY(0);
}

a:focus-visible,
button:focus-visible,
input:focus-visible,
select:focus-visible,
textarea:focus-visible {
  outline: 3px solid rgba(211, 170, 71, 0.58);
  outline-offset: 3px;
}

.site-header {
  position: sticky;
  z-index: 100;
  top: 0;
  border-bottom: 1px solid rgba(217, 214, 206, 0.9);
  background: rgba(248, 245, 238, 0.94);
  backdrop-filter: blur(16px);
}

.header-inner {
  display: grid;
  min-height: 84px;
  align-items: center;
  grid-template-columns: 1fr auto 1fr;
}

.wordmark {
  color: var(--sage);
  font-size: 2rem;
  font-weight: 700;
  letter-spacing: -0.065em;
}

.desktop-nav {
  display: flex;
  align-items: center;
  gap: 55px;
}

.desktop-nav a {
  position: relative;
  font-size: 0.89rem;
  font-weight: 600;
}

.desktop-nav a::after {
  position: absolute;
  right: 0;
  bottom: -8px;
  left: 0;
  height: 1px;
  background: var(--ink);
  content: "";
  transform: scaleX(0);
  transition: transform 0.2s ease;
}

.desktop-nav a:hover::after,
.desktop-nav a:focus-visible::after {
  transform: scaleX(1);
}

.header-actions {
  display: flex;
  justify-self: end;
  align-items: center;
  gap: 7px;
}

.header-cta {
  min-height: 40px;
  margin-left: 7px;
  padding: 9px 16px;
  border: 1px solid var(--ink);
  color: white;
  background: var(--ink);
  border-radius: 999px;
  cursor: pointer;
  font-size: 0.7rem;
  font-weight: 700;
}

.header-cta:hover {
  background: var(--forest);
}

.language-toggle {
  display: inline-grid;
  min-width: 42px;
  height: 34px;
  padding: 0 10px;
  border: 1px solid var(--line);
  place-items: center;
  color: var(--ink);
  background: rgba(255, 253, 248, 0.62);
  border-radius: 999px;
  cursor: pointer;
  font-size: 0.65rem;
  font-weight: 800;
  letter-spacing: 0.06em;
}

.language-toggle:hover {
  border-color: var(--forest);
  color: white;
  background: var(--forest);
}

.icon-button,
.menu-toggle {
  position: relative;
  display: grid;
  width: 42px;
  height: 42px;
  padding: 9px;
  border: 0;
  place-items: center;
  background: transparent;
  border-radius: 50%;
  cursor: pointer;
}

.icon-button:hover {
  background: rgba(144, 162, 126, 0.12);
}

.icon-button svg {
  width: 23px;
  fill: none;
  stroke: currentColor;
  stroke-linecap: round;
  stroke-linejoin: round;
  stroke-width: 1.5;
}

.bag-count {
  position: absolute;
  top: 0;
  right: -1px;
  display: grid;
  min-width: 18px;
  height: 18px;
  padding: 0 4px;
  place-items: center;
  color: white;
  background: var(--sage);
  border-radius: 999px;
  font-size: 0.62rem;
  font-weight: 700;
}

.menu-toggle {
  display: none;
}

.menu-toggle span {
  display: block;
  width: 22px;
  height: 1.5px;
  background: var(--ink);
  transition: transform 0.2s ease;
}

.menu-toggle span + span {
  margin-top: 6px;
}

.menu-toggle[aria-expanded="true"] span:first-child {
  transform: translateY(3.75px) rotate(45deg);
}

.menu-toggle[aria-expanded="true"] span:last-child {
  transform: translateY(-3.75px) rotate(-45deg);
}

.search-panel {
  position: absolute;
  top: 100%;
  right: 0;
  left: 0;
  padding: 20px 0;
  border-top: 1px solid var(--line);
  border-bottom: 1px solid var(--line);
  background: var(--paper);
  box-shadow: 0 12px 24px rgba(30, 35, 30, 0.06);
}

.search-panel[hidden],
.mobile-menu[hidden],
.modal[hidden],
.saved-drawer[hidden] {
  display: none;
}

.search-inner {
  display: grid;
  align-items: center;
  gap: 15px;
  grid-template-columns: auto 1fr auto;
}

.search-inner label {
  font-size: 0.78rem;
  font-weight: 700;
  text-transform: uppercase;
  letter-spacing: 0.08em;
}

.search-inner input {
  min-width: 0;
  padding: 12px 18px;
  border: 1px solid var(--line);
  outline: 0;
  background: var(--ivory);
  border-radius: 999px;
}

.text-button,
.view-all {
  padding: 5px 0;
  border: 0;
  background: transparent;
  cursor: pointer;
  font-size: 0.8rem;
  font-weight: 700;
  text-decoration: underline;
  text-underline-offset: 5px;
}

.mobile-menu {
  position: absolute;
  top: 100%;
  right: 0;
  left: 0;
  display: grid;
  padding: 26px 36px 30px;
  border-top: 1px solid var(--line);
  border-bottom: 1px solid var(--line);
  background: var(--paper);
  box-shadow: 0 15px 30px rgba(30, 35, 30, 0.08);
  gap: 18px;
}

.mobile-menu a {
  font-size: 1.05rem;
  font-weight: 600;
}

.shop-hero {
  border-bottom: 1px solid rgba(217, 214, 206, 0.72);
  background:
    radial-gradient(circle at 14% 60%, rgba(210, 194, 161, 0.13), transparent 35%),
    var(--ivory);
}

.hero-layout {
  display: grid;
  min-height: 610px;
  align-items: center;
  grid-template-columns: 0.78fr 1.42fr;
}

.hero-copy {
  position: relative;
  z-index: 2;
  max-width: 510px;
  padding: 70px 20px 70px 0;
}

.eyebrow,
.section-kicker {
  margin-bottom: 14px;
  color: var(--sage);
  font-size: 0.68rem;
  font-weight: 700;
  letter-spacing: 0.13em;
  text-transform: uppercase;
}

.hero-copy h1 {
  margin-bottom: 25px;
  font-size: clamp(3.1rem, 5vw, 5.2rem);
}

.hero-copy h1 em,
.lookbook-copy h2 em {
  color: var(--forest);
  font-family: var(--font-display);
  font-style: italic;
  font-weight: 600;
}

.hero-description {
  max-width: 430px;
  margin-bottom: 32px;
  color: var(--muted);
  font-size: 1rem;
}

.hero-actions {
  display: flex;
  flex-wrap: wrap;
  gap: 12px;
}

.button {
  display: inline-flex;
  min-height: 49px;
  padding: 13px 24px;
  border: 1px solid var(--ink);
  align-items: center;
  justify-content: center;
  gap: 18px;
  border-radius: 999px;
  cursor: pointer;
  font-size: 0.82rem;
  font-weight: 700;
  transition: transform 0.2s ease, box-shadow 0.2s ease, background 0.2s ease;
}

.button:hover {
  box-shadow: 0 10px 20px rgba(32, 35, 31, 0.13);
  transform: translateY(-2px);
}

.button--dark {
  color: white;
  background: var(--ink);
}

.button--dark:hover {
  background: var(--forest);
}

.button--outline {
  background: transparent;
}

.button--outline:hover {
  background: var(--paper);
}

.button--full {
  width: 100%;
}

.social-proof {
  display: flex;
  margin-top: 36px;
  align-items: center;
  gap: 10px;
  color: var(--muted);
  font-size: 0.72rem;
}

.avatar-stack {
  display: flex;
  padding-left: 8px;
}

.avatar-stack span {
  display: grid;
  width: 29px;
  height: 29px;
  margin-left: -8px;
  border: 2px solid var(--ivory);
  place-items: center;
  color: white;
  background: #75695e;
  border-radius: 50%;
  font-size: 0.63rem;
  font-weight: 700;
}

.avatar-stack span:nth-child(2) {
  background: #98806f;
}

.avatar-stack span:nth-child(3) {
  background: #64796d;
}

.proof-heart {
  color: #e2bd49;
}

.hero-image-wrap {
  position: relative;
  height: 100%;
  min-height: 610px;
  overflow: hidden;
}

.hero-image-wrap::before {
  position: absolute;
  z-index: 1;
  top: 0;
  bottom: 0;
  left: 0;
  width: 15%;
  background: linear-gradient(90deg, var(--ivory), transparent);
  content: "";
  pointer-events: none;
}

.hero-image-wrap img {
  width: 100%;
  height: 100%;
  object-fit: cover;
  object-position: 48% center;
}

.hero-note {
  position: absolute;
  z-index: 2;
  top: 40px;
  right: 30px;
  padding: 9px 14px;
  color: var(--forest);
  background: rgba(255, 253, 248, 0.87);
  border-radius: 999px;
  box-shadow: 0 8px 25px rgba(33, 40, 32, 0.08);
  backdrop-filter: blur(8px);
  font-size: 0.67rem;
  font-weight: 700;
  letter-spacing: 0.08em;
  text-transform: uppercase;
}

.hero-note i {
  color: var(--gold);
  font-style: normal;
}

.category-section {
  padding: 28px 0 54px;
}

.category-grid {
  display: grid;
  gap: 18px;
  grid-template-columns: repeat(4, 1fr);
}

.category-card {
  position: relative;
  display: flex;
  min-height: 190px;
  padding: 25px;
  border: 1px solid var(--line);
  overflow: hidden;
  border-radius: var(--radius);
  transition: transform 0.2s ease, box-shadow 0.2s ease;
}

.category-card:hover {
  box-shadow: var(--shadow);
  transform: translateY(-4px);
}

.category-card > div {
  position: relative;
  z-index: 2;
  width: 55%;
}

.category-card h3 {
  margin-bottom: 12px;
  font-size: 1.3rem;
  letter-spacing: -0.025em;
}

.category-card p {
  margin-bottom: 17px;
  font-size: 0.76rem;
}

.round-arrow {
  display: grid;
  width: 34px;
  height: 34px;
  border: 1px solid rgba(32, 35, 31, 0.25);
  place-items: center;
  background: rgba(255, 255, 255, 0.45);
  border-radius: 50%;
}

.category-card--desk {
  background: linear-gradient(140deg, #f4f0e6, #e7ebde);
}

.category-card--room {
  background: linear-gradient(140deg, #eff3f4, var(--blue-pale));
}

.category-card--fashion {
  background: linear-gradient(140deg, #f1efe8, #dedfd3);
}

.category-card--gift {
  background: linear-gradient(140deg, #f8f2eb, #f4e2dd);
}

.category-image {
  position: absolute;
  top: 0;
  right: 0;
  width: 47%;
  height: 100%;
  border-left: 1px solid rgba(32, 35, 31, 0.08);
  object-fit: cover;
  object-position: center;
  transition: transform 0.35s ease;
}

.category-card:hover .category-image {
  transform: scale(1.045);
}

.featured-section {
  padding: 20px 0 80px;
}

.validation-section {
  padding: 88px 0;
  border-top: 1px solid var(--line);
  border-bottom: 1px solid var(--line);
  background: var(--paper);
}

.validation-heading {
  display: grid;
  margin-bottom: 42px;
  align-items: end;
  gap: 22px 60px;
  grid-template-columns: 1.15fr 0.85fr;
}

.validation-heading .section-kicker {
  grid-column: 1 / -1;
  margin-bottom: -12px;
}

.validation-heading h2 {
  margin-bottom: 0;
  font-size: clamp(2.35rem, 4vw, 4rem);
}

.validation-heading > p:last-child {
  margin-bottom: 0;
  color: var(--muted);
  font-size: 0.9rem;
}

.validation-flow {
  display: grid;
  gap: 16px;
  grid-template-columns: repeat(3, 1fr);
}

.validation-flow article {
  position: relative;
  min-height: 225px;
  padding: 30px;
  border: 1px solid var(--line);
  overflow: hidden;
  background: var(--ivory);
  border-radius: var(--radius);
  transition: transform 0.25s ease, box-shadow 0.25s ease;
}

.validation-flow article:hover {
  box-shadow: 0 18px 42px rgba(38, 47, 38, 0.09);
  transform: translateY(-4px);
}

.validation-flow article:nth-child(2) {
  color: white;
  background: var(--forest);
  border-color: var(--forest);
}

.flow-card-top {
  display: flex;
  margin-bottom: 25px;
  align-items: flex-start;
  justify-content: space-between;
  gap: 18px;
}

.flow-number {
  display: block;
  padding-top: 6px;
  color: var(--sage);
  font-size: 0.63rem;
  font-weight: 700;
  letter-spacing: 0.1em;
  text-transform: uppercase;
}

.validation-flow article:nth-child(2) .flow-number {
  color: #d3ddca;
}

.flow-icon {
  display: grid;
  width: 66px;
  height: 66px;
  flex: 0 0 66px;
  place-items: center;
  color: var(--forest);
  background: #e8ecdf;
  border-radius: 20px;
  transform: rotate(2deg);
}

.flow-icon svg {
  width: 43px;
  height: 43px;
  stroke: currentColor;
  stroke-width: 2.2;
  stroke-linecap: round;
  stroke-linejoin: round;
}

.flow-icon--solution {
  color: #f5f1e8;
  background: rgba(255, 255, 255, 0.12);
  transform: rotate(-2deg);
}

.flow-icon--effect {
  color: #ba7068;
  background: #f5e3de;
}

.validation-flow h3 {
  margin-bottom: 11px;
  font-size: 1.35rem;
}

.validation-flow p {
  margin-bottom: 0;
  color: var(--muted);
  font-size: 0.76rem;
}

.validation-flow article:nth-child(2) p {
  color: #d4ddd8;
}

.validation-actions {
  display: flex;
  margin-top: 28px;
  justify-content: flex-end;
  gap: 10px;
}

.section-top {
  display: flex;
  margin-bottom: 24px;
  align-items: flex-end;
  justify-content: space-between;
  gap: 30px;
}

.section-top h2,
.lookbook h2,
.brand-story h2,
.join-strip h2 {
  margin-bottom: 9px;
  font-size: clamp(2rem, 3vw, 3rem);
}

.section-top h2 span {
  color: var(--gold);
}

.section-top p:not(.section-kicker) {
  margin-bottom: 0;
  color: var(--muted);
  font-size: 0.8rem;
}

.section-top .fx-note {
  max-width: 650px;
  margin-top: 6px;
  color: #92958f;
  font-size: 0.64rem;
}

.view-all {
  flex: 0 0 auto;
  text-decoration: none;
}

.filter-row {
  display: flex;
  margin-bottom: 23px;
  flex-wrap: wrap;
  gap: 8px;
}

.filter-chip {
  padding: 8px 15px;
  border: 1px solid var(--line);
  background: transparent;
  border-radius: 999px;
  cursor: pointer;
  font-size: 0.7rem;
  font-weight: 600;
}

.filter-chip:hover,
.filter-chip.is-active {
  border-color: var(--forest);
  color: white;
  background: var(--forest);
}

.store-grid {
  display: grid;
  gap: 15px;
  grid-template-columns: repeat(6, 1fr);
}

.home-preview-grid {
  grid-template-columns: repeat(3, 1fr);
}

.store-card {
  display: flex;
  min-width: 0;
  overflow: hidden;
  border: 1px solid var(--line);
  background: rgba(255, 253, 248, 0.66);
  border-radius: 15px;
  flex-direction: column;
  transition: box-shadow 0.2s ease, transform 0.2s ease;
}

.store-card:hover {
  box-shadow: 0 14px 32px rgba(38, 43, 36, 0.09);
  transform: translateY(-3px);
}

.store-image {
  position: relative;
  display: grid;
  height: 225px;
  overflow: hidden;
  place-items: center;
}

.product-detail-trigger {
  display: block;
  width: 100%;
  height: 100%;
  padding: 0;
  border: 0;
  overflow: hidden;
  background: transparent;
  cursor: zoom-in;
}

.store-product-image {
  width: 100%;
  height: 100%;
  object-fit: cover;
  transition: transform 0.22s ease;
}

.store-card:hover .store-product-image {
  transform: scale(1.035);
}

.status-tag {
  position: absolute;
  z-index: 3;
  top: 12px;
  left: 12px;
  padding: 6px 10px;
  color: white;
  background: var(--sage);
  border-radius: 999px;
  font-size: 0.55rem;
  font-weight: 700;
  letter-spacing: 0.04em;
}

.status-tag[data-status="Coming soon"] {
  background: #879eb8;
}

.status-tag[data-status="Seoul pick"] {
  color: #713f38;
  background: #edaaa0;
}

.heart-button {
  position: absolute;
  z-index: 3;
  top: 9px;
  right: 9px;
  display: grid;
  width: 36px;
  height: 36px;
  padding: 0;
  border: 0;
  place-items: center;
  background: rgba(255, 253, 248, 0.72);
  border-radius: 50%;
  cursor: pointer;
  font-size: 1.2rem;
  line-height: 1;
}

.heart-button.saved {
  color: #c56e67;
}

.store-info {
  display: flex;
  padding: 16px 14px 14px;
  flex: 1;
  flex-direction: column;
}

.store-info h3 {
  min-height: 50px;
  margin-bottom: 7px;
  font-size: 0.9rem;
  letter-spacing: -0.025em;
}

.maker {
  margin-bottom: 6px;
  color: #8b8d88;
  font-size: 0.57rem;
  font-weight: 600;
  letter-spacing: 0.04em;
  text-transform: uppercase;
}

.product-meta {
  display: flex;
  margin-bottom: 9px;
  align-items: center;
  gap: 6px;
  color: var(--forest);
  font-size: 0.57rem;
  font-weight: 700;
  text-transform: uppercase;
  letter-spacing: 0.04em;
}

.product-meta span {
  padding: 4px 7px;
  background: var(--sage-pale);
  border-radius: 999px;
}

.product-description {
  min-height: 53px;
  margin-bottom: 12px;
  color: var(--muted);
  font-size: 0.66rem;
  line-height: 1.45;
}

.original-price {
  display: block;
  margin-top: 2px;
  color: #969892;
  font-size: 0.56rem;
  font-weight: 500;
}

.source-link {
  color: var(--muted);
  font-size: 0.57rem;
  font-weight: 600;
  text-decoration: underline;
  text-underline-offset: 3px;
}

.card-bottom {
  display: flex;
  margin-top: auto;
  align-items: center;
  justify-content: space-between;
  gap: 8px;
}

.price {
  font-size: 0.78rem;
  font-weight: 700;
}

.card-actions {
  display: grid;
  margin-top: 12px;
  gap: 6px;
  grid-template-columns: 1fr 1fr;
}

.save-button,
.vote-button,
.details-button {
  min-height: 33px;
  padding: 6px 9px;
  border: 1px solid var(--line);
  background: transparent;
  border-radius: 999px;
  cursor: pointer;
  font-size: 0.6rem;
  font-weight: 700;
}

.save-button:hover,
.save-button.saved {
  border-color: #c98278;
  color: #8d4f47;
  background: #f5dfdb;
}

.details-button {
  display: grid;
  border-color: var(--forest);
  color: white;
  background: var(--forest);
  grid-column: 1 / -1;
  place-items: center;
  text-decoration: none;
}

.details-button:hover {
  background: #29483a;
}

.vote-button {
  width: 100%;
}

.vote-button:hover,
.vote-button.voted {
  border-color: var(--sage);
  color: white;
  background: var(--sage);
}

.vote-button:disabled {
  cursor: default;
}

.product-feedback {
  min-height: 21px;
  margin: 16px 0 0;
  color: var(--forest);
  font-size: 0.75rem;
  font-weight: 600;
  text-align: center;
}

.catalog-note {
  max-width: 780px;
  margin: 7px auto 0;
  color: #969892;
  font-size: 0.58rem;
  line-height: 1.5;
  text-align: center;
}

.ugc-section {
  padding: 85px 0;
  border-top: 1px solid var(--line);
  background: #eceee8;
}

.ugc-heading {
  display: flex;
  margin-bottom: 30px;
  align-items: flex-end;
  justify-content: space-between;
  gap: 30px;
}

.ugc-heading h2 {
  margin-bottom: 9px;
  font-size: clamp(2.2rem, 4vw, 4rem);
}

.ugc-heading p:not(.section-kicker) {
  max-width: 610px;
  margin-bottom: 0;
  color: var(--muted);
  font-size: 0.82rem;
}

.social-follow {
  flex: 0 0 auto;
  padding-bottom: 5px;
  border-bottom: 1px solid currentColor;
  font-size: 0.72rem;
  font-weight: 700;
}

.reel-grid {
  display: grid;
  gap: 16px;
  grid-template-columns: repeat(3, 1fr);
}

.reel-card {
  position: relative;
  display: block;
  aspect-ratio: 9 / 14;
  padding: 0;
  border: 0;
  overflow: hidden;
  color: white;
  background: var(--forest);
  border-radius: 24px;
  cursor: pointer;
  text-align: left;
  box-shadow: 0 14px 34px rgba(39, 48, 40, 0.11);
}

.reel-card img,
.reel-card video {
  width: 100%;
  height: 100%;
  object-fit: cover;
  transition: transform 0.7s cubic-bezier(0.2, 0.7, 0.2, 1);
}

.reel-card:hover img,
.reel-card:focus-visible img,
.reel-card:hover video,
.reel-card:focus-within video {
  transform: scale(1.07);
}

.reel-shade {
  position: absolute;
  z-index: 1;
  inset: 0;
  background: linear-gradient(180deg, rgba(20, 24, 20, 0.32), transparent 36%, rgba(15, 19, 16, 0.76));
  pointer-events: none;
}

.reel-top,
.reel-copy {
  position: absolute;
  z-index: 3;
  right: 18px;
  left: 18px;
}

.reel-top {
  top: 17px;
  display: flex;
  align-items: center;
  justify-content: space-between;
  gap: 12px;
}

.reel-top b {
  padding: 6px 9px;
  background: rgba(25, 30, 25, 0.58);
  border-radius: 999px;
  backdrop-filter: blur(8px);
  font-size: 0.58rem;
  letter-spacing: 0.05em;
  text-transform: uppercase;
}

.reel-top small {
  font-size: 0.62rem;
}

.reel-play {
  position: absolute;
  z-index: 1;
  top: 50%;
  left: 50%;
  display: grid;
  width: 52px;
  height: 52px;
  padding-left: 3px;
  place-items: center;
  background: rgba(255, 253, 248, 0.86);
  border-radius: 50%;
  color: var(--forest);
  font-size: 0.9rem;
  transform: translate(-50%, -50%);
  transition: transform 0.25s ease;
}

.reel-card:hover .reel-play {
  transform: translate(-50%, -50%) scale(1.08);
}

.reel-copy {
  bottom: 19px;
}

.reel-copy strong,
.reel-copy small {
  display: block;
}

.reel-copy strong {
  margin-bottom: 6px;
  font-size: 1rem;
  line-height: 1.25;
}

.reel-copy small {
  color: rgba(255, 255, 255, 0.78);
  font-size: 0.64rem;
}

.reel-credit-link {
  position: absolute;
  z-index: 2;
  inset: 0;
}

.video-sound-toggle {
  position: absolute;
  z-index: 4;
  top: 52px;
  right: 17px;
  padding: 7px 10px;
  border: 1px solid rgba(255, 255, 255, 0.46);
  color: white;
  background: rgba(25, 30, 25, 0.58);
  border-radius: 999px;
  cursor: pointer;
  backdrop-filter: blur(8px);
  font: inherit;
  font-size: 0.58rem;
  font-weight: 700;
}

.video-sound-toggle:hover,
.video-sound-toggle:focus-visible {
  background: rgba(25, 30, 25, 0.84);
}

.ugc-disclaimer {
  max-width: 760px;
  margin: 18px auto 0;
  color: #858982;
  font-size: 0.6rem;
  text-align: center;
}

.lookbook-section {
  padding: 72px 0;
}

.lookbook {
  display: grid;
  min-height: 310px;
  overflow: hidden;
  background: var(--paper);
  border-radius: 22px;
  box-shadow: inset 0 0 0 1px var(--line);
  grid-template-columns: 1.3fr 1fr 1fr 1fr;
}

.lookbook-copy {
  display: flex;
  padding: 44px 40px;
  align-items: flex-start;
  justify-content: center;
  flex-direction: column;
}

.lookbook-copy h2 {
  font-size: 2rem;
}

.lookbook-copy p:not(.section-kicker) {
  max-width: 270px;
  margin-bottom: 22px;
  color: var(--muted);
  font-size: 0.8rem;
}

.lookbook-copy .button {
  min-height: 42px;
  padding: 10px 18px;
  font-size: 0.7rem;
}

.lookbook-panel {
  position: relative;
  display: grid;
  overflow: hidden;
  place-items: center;
}

.lookbook-panel img {
  width: 100%;
  height: 100%;
  object-fit: cover;
  transition: transform 0.45s ease;
}

.lookbook-panel:hover img {
  transform: scale(1.035);
}

.brand-story {
  padding: 72px 0 85px;
  border-top: 1px solid var(--line);
  background: var(--paper);
}

.story-layout {
  display: grid;
  align-items: start;
  gap: 55px;
  grid-template-columns: 0.8fr 0.9fr 2.3fr;
}

.story-title h2 {
  font-size: 2.1rem;
}

.story-mark {
  display: grid;
  width: 58px;
  height: 58px;
  margin-top: 22px;
  place-items: center;
  color: var(--sage);
  background: #e9eee3;
  border-radius: 18px;
  transform: rotate(-4deg);
}

.story-mark svg {
  width: 38px;
  height: 38px;
  stroke: currentColor;
  stroke-width: 2.4;
  stroke-linecap: round;
  stroke-linejoin: round;
}

.story-copy {
  padding-top: 7px;
}

.story-copy p {
  color: var(--muted);
  font-size: 0.76rem;
}

.story-copy a {
  font-size: 0.72rem;
  font-weight: 700;
  text-decoration: underline;
  text-underline-offset: 5px;
}

.value-grid {
  display: grid;
  gap: 28px;
  grid-template-columns: repeat(4, 1fr);
}

.value-grid article {
  text-align: center;
}

.value-icon {
  display: grid;
  width: 64px;
  height: 64px;
  margin: 0 auto 18px;
  place-items: center;
  background: currentColor;
  border-radius: 22px;
  transform: rotate(3deg);
  transition: transform 0.25s ease;
}

.value-grid article:nth-child(even) .value-icon {
  transform: rotate(-3deg);
}

.value-grid article:hover .value-icon {
  transform: translateY(-4px) rotate(0);
}

.value-icon svg {
  width: 37px;
  height: 37px;
  color: white;
  stroke: currentColor;
  stroke-width: 2.2;
  stroke-linecap: round;
  stroke-linejoin: round;
}

.value-icon--green { color: #8fab83; }
.value-icon--blue { color: #8aa9cc; }
.value-icon--coral { color: #e49189; }
.value-icon--gold { color: #e0b43c; }

.value-grid h3 {
  margin-bottom: 10px;
  font-size: 0.82rem;
  letter-spacing: -0.02em;
}

.value-grid p {
  margin-bottom: 0;
  color: var(--muted);
  font-size: 0.65rem;
}

.join-strip {
  padding: 75px 0;
  color: white;
  background: var(--forest);
}

.join-inner {
  display: flex;
  align-items: center;
  justify-content: space-between;
  gap: 35px;
}

.join-strip .section-kicker {
  color: #cbd5bf;
}

.join-strip h2 {
  margin-bottom: 0;
}

.join-strip .button {
  border-color: white;
  color: var(--ink);
  background: white;
}

.site-footer {
  background: var(--ivory);
}

.footer-main {
  display: grid;
  padding: 65px 0 55px;
  gap: 50px;
  grid-template-columns: 1.5fr 0.7fr 1fr;
}

.footer-main p {
  margin-top: 10px;
  color: var(--muted);
  font-size: 0.76rem;
}

.footer-main nav,
.footer-contact {
  display: grid;
  align-content: start;
  gap: 10px;
  font-size: 0.77rem;
}

.footer-contact {
  justify-items: end;
}

.social-coming-soon {
  color: var(--muted);
}

.footer-promise {
  border-top: 1px solid var(--line);
}

.footer-promise .shell {
  display: flex;
  min-height: 56px;
  align-items: center;
  justify-content: space-between;
  color: var(--muted);
  font-size: 0.68rem;
}

.saved-drawer,
.modal {
  position: fixed;
  z-index: 500;
  inset: 0;
}

.drawer-backdrop,
.modal-backdrop {
  position: absolute;
  inset: 0;
  background: rgba(24, 28, 24, 0.54);
  backdrop-filter: blur(3px);
}

.drawer-panel {
  position: absolute;
  top: 0;
  right: 0;
  display: flex;
  width: min(100%, 430px);
  height: 100%;
  padding: 32px;
  background: var(--paper);
  box-shadow: -20px 0 50px rgba(23, 28, 23, 0.16);
  flex-direction: column;
  animation: drawer-in 0.25s ease-out;
}

@keyframes drawer-in {
  from { transform: translateX(100%); }
  to { transform: translateX(0); }
}

.drawer-header {
  display: flex;
  padding-bottom: 18px;
  border-bottom: 1px solid var(--line);
  align-items: flex-start;
  justify-content: space-between;
}

.drawer-header h2 {
  margin-bottom: 0;
  font-size: 2rem;
}

.drawer-close,
.modal-close {
  display: grid;
  width: 38px;
  height: 38px;
  padding: 0;
  border: 1px solid var(--line);
  place-items: center;
  background: transparent;
  border-radius: 50%;
  cursor: pointer;
  font-size: 1.35rem;
}

.saved-list {
  display: grid;
  margin: 20px 0;
  overflow-y: auto;
  gap: 10px;
}

.saved-item {
  display: grid;
  padding: 10px;
  align-items: center;
  gap: 12px;
  background: var(--ivory);
  border-radius: 12px;
  grid-template-columns: 55px 1fr auto;
}

.saved-item-icon {
  display: grid;
  width: 55px;
  height: 55px;
  place-items: center;
  background: var(--sage-pale);
  border-radius: 9px;
  font-size: 1.8rem;
  object-fit: cover;
}

.saved-item strong,
.saved-item small {
  display: block;
}

.saved-item strong {
  font-size: 0.76rem;
}

.saved-item small {
  color: var(--muted);
  font-size: 0.67rem;
}

.remove-saved {
  padding: 7px;
  border: 0;
  color: var(--muted);
  background: transparent;
  cursor: pointer;
  font-size: 1rem;
}

.empty-saved {
  padding: 50px 10px;
  color: var(--muted);
  font-size: 0.82rem;
  text-align: center;
}

.drawer-panel > .button {
  margin-top: auto;
}

.modal {
  display: grid;
  padding: 24px;
  place-items: center;
}

.modal-panel {
  position: relative;
  z-index: 1;
  width: min(100%, 500px);
  padding: 43px;
  background: var(--paper);
  border-radius: 22px;
  box-shadow: 0 24px 70px rgba(17, 22, 18, 0.26);
}

.product-detail-panel {
  display: grid;
  width: min(100%, 920px);
  max-height: calc(100vh - 48px);
  padding: 0;
  overflow-y: auto;
  grid-template-columns: 0.95fr 1.05fr;
}

.product-detail-visual {
  position: relative;
  min-height: 560px;
  overflow: hidden;
  background: var(--ivory);
  border-radius: 22px 0 0 22px;
}

.product-detail-visual img {
  width: 100%;
  height: 100%;
  object-fit: cover;
}

.product-detail-copy {
  padding: 54px 48px 42px;
}

.product-detail-copy h2 {
  margin-bottom: 5px;
  font-size: clamp(2rem, 4vw, 3.4rem);
}

.detail-original-name {
  margin-bottom: 22px;
  color: #92958f;
  font-size: 0.67rem;
}

.product-detail-copy > p:not(.section-kicker, .detail-original-name, .detail-note, .detail-feedback) {
  color: var(--muted);
  font-size: 0.84rem;
}

.detail-facts {
  display: grid;
  margin: 26px 0 16px;
  border-top: 1px solid var(--line);
}

.detail-facts div {
  display: grid;
  padding: 12px 0;
  border-bottom: 1px solid var(--line);
  gap: 16px;
  grid-template-columns: 110px 1fr;
}

.detail-facts dt {
  color: var(--muted);
  font-size: 0.67rem;
}

.detail-facts dd {
  margin: 0;
  font-size: 0.72rem;
  font-weight: 700;
}

.detail-note,
.prototype-note {
  color: #92958f;
  font-size: 0.6rem;
  line-height: 1.5;
}

.detail-actions {
  display: grid;
  margin-top: 24px;
  gap: 9px;
  grid-template-columns: 1fr 1fr;
}

.detail-actions .button {
  min-height: 48px;
  padding: 12px 14px;
  font-size: 0.65rem;
}

#detail-interest-button.is-registered {
  color: var(--forest);
  background: var(--sage-pale);
}

.detail-feedback {
  min-height: 20px;
  margin: 11px 0 0;
  color: var(--forest);
  font-size: 0.66rem;
  font-weight: 600;
}

.prototype-note {
  margin: 10px 0 0;
  text-align: center;
}

.modal-panel h2 {
  margin-bottom: 13px;
  font-size: 2.4rem;
}

.modal-panel > p:not(.section-kicker) {
  color: var(--muted);
  font-size: 0.85rem;
}

.modal-close {
  position: absolute;
  top: 15px;
  right: 15px;
}

.modal form {
  margin-top: 25px;
}

.modal label {
  display: block;
  margin-bottom: 7px;
  font-size: 0.73rem;
  font-weight: 700;
}

.modal input {
  width: 100%;
  margin-bottom: 13px;
  padding: 13px 15px;
  border: 1px solid var(--line);
  outline: 0;
  background: var(--ivory);
  border-radius: 9px;
}

.modal select,
.modal textarea {
  width: 100%;
  margin-bottom: 13px;
  padding: 13px 15px;
  border: 1px solid var(--line);
  outline: 0;
  color: var(--ink);
  background: var(--ivory);
  border-radius: 9px;
}

.modal textarea {
  resize: vertical;
}

.modal-price-input {
  display: flex;
  margin-bottom: 13px;
  border: 1px solid var(--line);
  align-items: center;
  background: var(--ivory);
  border-radius: 9px;
}

.modal-price-input span {
  padding-left: 15px;
  color: var(--muted);
}

.modal-price-input input {
  margin-bottom: 0;
  border: 0;
  background: transparent;
}

.js [data-reveal] {
  opacity: 0;
  transform: translateY(20px);
  transition: opacity 0.65s ease, transform 0.65s ease;
}

.js [data-reveal].is-visible {
  opacity: 1;
  transform: translateY(0);
}

.form-message {
  min-height: 20px;
  margin: 11px 0 0;
  color: var(--forest);
  font-size: 0.75rem;
  font-weight: 600;
  text-align: center;
}

@media (max-width: 1180px) {
  .desktop-nav {
    gap: 30px;
  }

  .hero-layout {
    grid-template-columns: 0.9fr 1.1fr;
  }

  .store-grid {
    grid-template-columns: repeat(3, 1fr);
  }

  .story-layout {
    grid-template-columns: 1fr 1fr;
  }

  .value-grid {
    grid-column: 1 / -1;
  }
}

@media (max-width: 900px) {
  .shell {
    width: min(calc(100% - 40px), var(--shell));
  }

  .header-inner {
    min-height: 72px;
    grid-template-columns: 1fr auto;
  }

  .desktop-nav {
    display: none;
  }

  .account-button {
    display: none;
  }

  .header-cta {
    display: none;
  }

  .menu-toggle {
    display: grid;
  }

  .hero-layout {
    min-height: auto;
    grid-template-columns: 1fr;
  }

  .hero-copy {
    max-width: 700px;
    padding: 65px 0 50px;
  }

  .hero-image-wrap {
    min-height: 500px;
    border-radius: 20px 20px 0 0;
  }

  .hero-image-wrap::before {
    display: none;
  }

  .category-grid {
    grid-template-columns: repeat(2, 1fr);
  }

  .validation-heading {
    grid-template-columns: 1fr;
  }

  .validation-flow {
    grid-template-columns: 1fr;
  }

  .validation-flow article {
    min-height: 190px;
  }

  .reel-grid {
    grid-template-columns: repeat(2, 1fr);
  }

  .product-detail-panel {
    grid-template-columns: 1fr;
  }

  .product-detail-visual {
    min-height: 390px;
    border-radius: 22px 22px 0 0;
  }

  .lookbook {
    grid-template-columns: 1fr 1fr;
  }

  .lookbook-copy {
    grid-row: span 2;
  }

  .lookbook-panel {
    min-height: 220px;
  }

  .panel-three {
    display: none;
  }
}

@media (max-width: 640px) {
  .shell {
    width: min(calc(100% - 28px), var(--shell));
  }

  .wordmark {
    font-size: 1.7rem;
  }

  .icon-button,
  .menu-toggle {
    width: 38px;
    height: 38px;
    padding: 8px;
  }

  .search-inner {
    grid-template-columns: 1fr auto;
  }

  .search-inner label {
    grid-column: 1 / -1;
  }

  .hero-copy h1 {
    font-size: 3.05rem;
  }

  .hero-actions {
    align-items: stretch;
    flex-direction: column;
  }

  .hero-actions .button {
    width: 100%;
  }

  .social-proof {
    flex-wrap: wrap;
  }

  .hero-image-wrap {
    min-height: 390px;
  }

  .hero-image-wrap img {
    object-position: 52% center;
  }

  .hero-note {
    top: 18px;
    right: 16px;
  }

  .category-grid {
    display: flex;
    margin-right: -14px;
    padding-right: 14px;
    overflow-x: auto;
    scroll-snap-type: x mandatory;
  }

  .category-card {
    min-width: 275px;
    scroll-snap-align: start;
  }

  .section-top {
    align-items: flex-start;
  }

  .filter-row {
    margin-right: -14px;
    padding: 0 14px 5px 0;
    overflow-x: auto;
    flex-wrap: nowrap;
  }

  .filter-chip {
    flex: 0 0 auto;
  }

  .store-grid {
    grid-template-columns: repeat(2, 1fr);
  }

  .store-image {
    height: 190px;
  }

  .ugc-section {
    padding: 68px 0;
  }

  .ugc-heading {
    align-items: flex-start;
    flex-direction: column;
  }

  .reel-grid {
    display: flex;
    margin-right: -14px;
    padding-right: 14px;
    overflow-x: auto;
    scroll-snap-type: x mandatory;
  }

  .reel-card {
    min-width: min(76vw, 290px);
    scroll-snap-align: start;
  }

  .product-detail-copy {
    padding: 42px 22px 28px;
  }

  .product-detail-visual {
    min-height: 340px;
  }

  .detail-actions {
    grid-template-columns: 1fr;
  }

  .lookbook {
    display: block;
  }

  .lookbook-copy {
    padding: 38px 26px;
  }

  .lookbook-panel {
    min-height: 240px;
  }

  .panel-three {
    display: grid;
  }

  .story-layout {
    grid-template-columns: 1fr;
  }

  .validation-actions {
    align-items: stretch;
    flex-direction: column;
  }

  .validation-actions .button {
    width: 100%;
  }

  .value-grid {
    grid-column: auto;
    grid-template-columns: repeat(2, 1fr);
  }

  .join-inner {
    align-items: stretch;
    flex-direction: column;
  }

  .footer-main {
    grid-template-columns: 1fr;
  }

  .footer-contact {
    justify-items: start;
  }

  .footer-promise .shell {
    padding: 18px 0;
    align-items: flex-start;
    flex-direction: column;
    gap: 8px;
  }

  .modal {
    padding: 14px;
  }

  .modal-panel {
    padding: 38px 22px 25px;
  }
}

@media (max-width: 420px) {
  .search-toggle {
    display: none;
  }

  .hero-copy h1 {
    font-size: 2.7rem;
  }

  .store-grid {
    grid-template-columns: 1fr;
  }

  .store-image {
    height: 260px;
  }
}

@media (prefers-reduced-motion: reduce) {
  *,
  *::before,
  *::after {
    scroll-behavior: auto !important;
    animation-duration: 0.01ms !important;
    animation-iteration-count: 1 !important;
    transition-duration: 0.01ms !important;
  }

  .js [data-reveal] {
    opacity: 1;
    transform: none;
  }
}
